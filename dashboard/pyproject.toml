[project]
name = "specforge-dashboard"
version = "0.1.0"
description = "SpecForge AI dashboard"
requires-python = ">=3.12"
dependencies = [
  "requests>=2.32.3",
  "streamlit>=1.44.0"
]

[build-system]
requires = ["setuptools>=75.8.0", "wheel"]
build-backend = "setuptools.build_meta"

